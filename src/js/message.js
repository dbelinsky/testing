// eslint-disable-next-line import/prefer-default-export
export default function displayValidationMessage (element, message) {
    // eslint-disable-next-line no-param-reassign
    element.textContent = message;
}
